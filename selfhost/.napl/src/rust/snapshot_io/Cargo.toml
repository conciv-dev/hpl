# GENERATED BY NAPL from snapshot_io.napl — edit the prompt, not this file
[package]
name = "snapshot_io"
version = "0.1.0"
edition = "2021"

[dependencies]
hash = { path = "../hash" }
snapshot_filter = { path = "../snapshot_filter" }
