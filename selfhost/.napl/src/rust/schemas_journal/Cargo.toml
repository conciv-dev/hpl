[package]
name = "schemas_journal"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
text_diff = { path = "../text_diff" }
blame = { path = "../blame" }
