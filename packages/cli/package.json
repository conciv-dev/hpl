{
  "name": "@napl/cli",
  "version": "0.1.0",
  "description": "NAPL CLI: the `napl` command (init, gen, status, test, blame).",
  "type": "module",
  "license": "MIT",
  "bin": {
    "napl": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && chmod +x dist/cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@napl/core": "workspace:*",
    "commander": "^15.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
